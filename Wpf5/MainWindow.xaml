<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Margin="10">Ma San Pham: </Label>
        <TextBox x:Name="txtMa" Grid.Row="0" Grid.Column="1" Width="200" Height="30"
                 HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center"
                 Style="{StaticResource textStyle}"/>

        <Label Grid.Row="1" Margin="10">Ten San Pham:</Label>
        <TextBox x:Name="txtTen" Grid.Row="1" Grid.Column="1" Width="300" Height="30"
                HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center"
                 Style="{StaticResource textStyle}"/>

        <Label Grid.Row="2" Margin="10">Loai San Pham:</Label>
        <ComboBox Name="cboLoai" Grid.Row="2" Grid.Column="1" Width="250" Height="30"
                HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center"/>

        <Label Grid.Row="3" Margin="10">Don Gia:</Label>
        <TextBox x:Name="txtDonGia" Grid.Row="3" Grid.Column="1" Width="200" Height="30"
                HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center"
                 Style="{StaticResource textStyle}"/>

        <Label Grid.Row="4" Margin="10">So Luong:</Label>
        <TextBox x:Name="txtSoLuong" Grid.Row="4" Grid.Column="1" Width="200" Height="30"
                HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center"
                 Style="{StaticResource textStyle}"/>

        <DataGrid x:Name="dtgSanPham" Grid.Row="5" Grid.ColumnSpan="2"
                  Margin="10" AutoGenerateColumns="False"
                  ColumnWidth="*" ColumnHeaderStyle="{StaticResource dataHeaderStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ma Sp" Binding="{Binding MaSp}"/>
                <DataGridTextColumn Header="Ten Sp" Binding="{Binding TenSp}"/>
                <DataGridTextColumn Header="Ma Loai" Binding="{Binding MaLoai}"/>
                <DataGridTextColumn Header="So Luong Co" Binding="{Binding SoLuong}"/>
                <DataGridTextColumn Header="Don Gia" Binding="{Binding Don Gia}"/>
                <DataGridTextColumn Header="Thanh Tien" Binding="{Binding ThanhTien, StringFormat=N0}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Center"
                    Margin="10">
            <Button x:Name="btnThem" Width="50" Height="30" Margin="10"
                    Click="btnThem_Click"
                    Template="{StaticResource btnTemp}">Them</Button>
            <Button x:Name="btnSua" Width="50" Height="30" Margin="10"
                    Click="btnSua_Click"
                    Template="{StaticResource btnTemp}">Sua</Button>
            <Button x:Name="btnXoa" Width="50" Height="30" Margin="10"
                    Click="btnXoa_Click"
                    Template="{StaticResource btnTemp}">Xoa</Button>
            <Button x:Name="btnTim" Width="50" Height="30" Margin="10"
                    Click="btnTim_Click"
                    Template="{StaticResource btnTemp}">Tim</Button>
        </StackPanel>
    </Grid>
</Window>
